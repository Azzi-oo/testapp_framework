[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --alluredir=allure-results
markers =
    sorting: Tests for category sorting functionality
    search_cart: Tests for search and cart functionality
    cart: Tests for cart manipulation functionality
